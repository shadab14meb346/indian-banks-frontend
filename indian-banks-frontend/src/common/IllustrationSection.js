import React from 'react';
import BankDetails from '../common/BankDetails';
import '../styles/illustration-section.scss';

const IllustrationSection = () => {
	return (
		<div className="illustration-section">
			<BankDetails />
		</div>
	);
};

export default IllustrationSection;
